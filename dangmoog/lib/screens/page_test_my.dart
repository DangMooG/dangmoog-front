import 'package:flutter/material.dart';

class PageTestMy extends StatelessWidget {
  const PageTestMy({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('My Page')),
    );
  }
}
