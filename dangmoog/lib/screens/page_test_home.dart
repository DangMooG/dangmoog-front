import 'package:flutter/material.dart';

class PageTestHome extends StatelessWidget {
  const PageTestHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Home Page'));
  }
}
